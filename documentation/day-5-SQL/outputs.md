# Day 5 Outputs

## Practical Results

### Schema Design
- Successfully created relational database tables.

### Foreign Key
- Verified foreign key constraint by attempting to insert an invalid `user_id` (received expected constraint error).

### INNER JOIN
- Successfully displayed matching user and project records.

### LEFT JOIN
- Successfully displayed all users, including users without projects.

### RIGHT JOIN
- Successfully displayed project records with matching users.

### Subquery
- Retrieved projects for a specific user.

### CTE
- Executed Common Table Expression successfully.

### Window Functions
- Used ROW_NUMBER(), RANK(), and DENSE_RANK() for employee ranking.

### Index
- Created and verified index using `SHOW INDEX`.

### SQL Assignment
- Completed 25 SQL practice queries.